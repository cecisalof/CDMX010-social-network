// import { formatters } from "stylelint";
// import { auth } from '../lib/firebase.js';

// // función registro de usuarios con correo y contraseña
// auth.createUserWithEmailAndPassword(email, password)
//   .then((user) => {
//   // firebase envía un mail de verificación de registro de cuenta
//   // al correo del usuario
//     user.sendSignInLinkToEmail().catch(error => {
//       console.log(error);
//     });
//   })

// // código para que un usuario pueda acceder con correo y contraseña
// firebase.auth().signInWithEmailAndPassword(email, password)
//   .then((user) => {
//     // Signed in
//     // ...
//   })
//   .catch((error) => {
//     let errorCode = error.code;
//     let errorMessage = error.message;
//   });
